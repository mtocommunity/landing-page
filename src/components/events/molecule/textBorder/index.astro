---
/*!
 * File of ManyToOne Community
 * Licensed under the GPL-3.0 or later License: https://github.com/mtocommunity/landing-page/blob/master/LICENSE.md
 */

import "./styles.css";

type Props = {
  title: string;
  className?: string;
  color: "purple" | string;
};

const { title, color, className } = Astro.props;
---

<span class={`${className} text-border-${color}`}>{title}</span>
